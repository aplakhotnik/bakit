# Inputs

Place raw source material here (notes, documents, transcripts) as text-based files.
Skills read everything in this folder as the starting material for analysis.

In this example, `stakeholder-brief.md` is the single input the analyst was given.
